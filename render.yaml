services:
  - type: web
    name: spotify-discovery-pm
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MOCK_MODE
        value: true
      - key: PYTHONPATH
        value: .
