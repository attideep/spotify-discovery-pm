{
  "version": 2,
  "builds": [
    { "src": "api/main.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/web/$1" },
    { "src": "/(.*)", "dest": "api/main.py" }
  ],
  "env": {
    "MOCK_MODE": "true"
  }
}
